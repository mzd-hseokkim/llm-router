server:
  port: 8080
  read_timeout: 30s
  write_timeout: 60s

database:
  url: "postgres://llmrouter:llmrouter@localhost:5433/llmrouter?sslmode=disable"
  max_connections: 20

redis:
  addr: "localhost:6380"

log:
  level: "info"
  format: "text"
