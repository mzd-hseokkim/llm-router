# Code Review: ATL-284 (2차)
## Admin UI: 프롬프트 관리 페이지 (/prompts)

**작성일**: 2026-02-23
**Branch**: feature/ATL-284
**결과**: Approve
**리뷰 파일 수**: 3 (page.tsx, PromptsPage.test.tsx, Sidebar.tsx)

---

## Gap Analysis

설계 문서 `docs/design/ATL-284.design.md` Implementation Plan 전체 항목 확인.

| Design 항목 | 구현 여부 | 파일 위치 | 비고 |
|---|---|---|---|
| Step 1: Sidebar 링크 추가 | ✅ | `Sidebar.tsx:12` | |
| Step 2: PromptsPage 기본 구조 (목록/로딩/빈 상태) | ✅ | `page.tsx:615–736` | |
| Step 3: VariableEditor (textarea+overlay, 스크롤 동기화) | ✅ | `page.tsx:84–149` | |
| Step 4: CreatePromptDialog | ✅ | `page.tsx:394–505` | |
| Step 5: EditPromptDialog (버전 자동 제안 포함) | ✅ | `page.tsx:509–608`, `suggestNextVersion:32–37` | 1차 W-2/W-3 해소 |
| Step 6: VersionPanel + DiffView | ✅ | `page.tsx:153–390` | |
| Step 7: 테스트 작성 (TC-1 ~ TC-14 전체) | ✅ | `PromptsPage.test.tsx` | 1차 W-1 해소 |

**설계-구현 매칭률: 100% (7/7)**

---

## 1차 리뷰 Warning 해소 확인

| Warning | 상태 | 수정 내용 |
|---|---|---|
| W-1: TC-4, TC-5 누락 | ✅ 해소 | `PromptsPage.test.tsx:92–126` — 생성 성공/실패 경로 모두 검증, 31/31 통과 |
| W-2: 버전 자동 제안 미구현 | ✅ 해소 | `suggestNextVersion()` 헬퍼 추가, `useEffect`로 versions 첫 로드 시 자동 채움 |
| W-3: `currentVersion={null}` 고정 | ✅ 해소 | `useEffect` 내 `is_active` 버전 template으로 초기화 |

---

## Code Quality Review

### Critical
없음.

### Warnings
없음.

### Info (잔여, 비차단)

**I-1: DiffView — 라인 번호 미표시**
- `page.tsx:175–195`: +/−/ ` ` 기호만 표시, 실제 라인 번호(1, 2, 3…) 없음
- 기능적 문제 없음. 향후 UX 개선 시 대응 가능

**I-2: `escapeHtml` — quote 미이스케이프**
- `page.tsx:11–16`: `"`, `'` 미처리
- 현재 innerHTML 텍스트 노드 컨텍스트에서 XSS 위험 없음

**I-3: LCS 알고리즘 — 런타임 가드 없음**
- `page.tsx:41–78`: 설계 문서에 500라인 이하 상정으로 문서화됨
- MVP 범위에서 수용 가능

---

## `suggestNextVersion` 코드 검증

```typescript
function suggestNextVersion(versionStrings: string[]): string {
  if (versionStrings.length === 0) return "";
  const last = versionStrings[versionStrings.length - 1];
  const match = last.match(/^(.*?)(\d+)([^0-9]*)$/);
  return match ? `${match[1]}${parseInt(match[2]) + 1}${match[3]}` : "";
}
```

| 입력 | 출력 | 검증 |
|---|---|---|
| `["1.0.0"]` | `"1.0.1"` | ✅ |
| `["v2"]` | `"v3"` | ✅ |
| `["1.0.0-beta"]` | `"1.0.1-beta"` | ✅ |
| `["abc"]` (숫자 없음) | `""` → version 미업데이트 | ✅ |
| `[]` | `""` → version 미업데이트 | ✅ |

---

## Positive Notes

- ✅ 1차 리뷰 W-1/W-2/W-3 모두 깔끔하게 해소
- ✅ `useRef` 패턴으로 `useEffect` 1회 실행 보장 — 사용자 편집 덮어쓰기 없음
- ✅ `suggestNextVersion` 정규식이 semver, prefix+숫자, suffix 있는 버전 모두 올바르게 처리
- ✅ TC-4/TC-5 — `prompts.create` 호출 인자 검증 + 성공/실패 경로 모두 커버
- ✅ 전체 31/31 테스트 통과

---

Reviewed by Claude Code
