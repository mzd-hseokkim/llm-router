# Code Review: ATL-287

**날짜**: 2026-02-24
**결과**: Approve
**리뷰 파일 수**: 4 (Sidebar.tsx, api.ts, reports/page.tsx, ReportsPage.test.tsx)
**커밋 수**: 미커밋(작업 중인 변경사항)

---

## Gap Analysis

설계 문서(플랜 기반) 대비 구현 매칭률: **93% (14/15)**

| 계획 항목 | 구현 여부 | 파일 위치 | 비고 |
|---|---|---|---|
| T1. `ChargebackBreakdown` 타입 제거 및 재정의 | ✅ | `lib/api.ts:731-783` | `ChargebackSummary`, `TeamChargebackEntry`, `BillingModelBreakdown`, `BillingTagBreakdown` 신규 정의 |
| T1. `ShowbackReport` 타입 백엔드 일치 수정 | ✅ | `lib/api.ts:762-783` | 필드 전면 재정의 |
| T1. `reports.showback()` period 쿼리 파람 | ✅ | `lib/api.ts:789-793` | `URLSearchParams` 사용 |
| T2. Sidebar "Reports" 링크 (Usage 다음) | ✅ | `components/Sidebar.tsx:16` | 정확한 위치에 삽입 |
| T3. Header + Period Selector | ✅ | `reports/page.tsx:55-66` | 현재 월 기본값 |
| T3. Chargeback 탭 (Summary Cards 3종) | ✅ | `reports/page.tsx:95-109` | `StatCard` 재사용 |
| T3. Chargeback 팀별 테이블 (6컬럼) | ✅ | `reports/page.tsx:130-165` | Cost/Markup/Total/Tokens/Requests |
| T3. CSV 내보내기 버튼 | ✅ | `reports/page.tsx:35-50, 116-122` | Blob download 구현 |
| T3. Showback 탭 + Team Selector | ✅ | `reports/page.tsx:175-248` | `teams.list()` 연동 |
| T3. Showback 모델별 테이블 | ✅ | `reports/page.tsx:214-243` | `by_model` 조건부 렌더링 |
| Edge Case: 빈 `by_team` → "데이터 없음" | ✅ | `reports/page.tsx:124-128` | 중앙 메시지 표시 |
| Edge Case: 팀 미선택 안내 메시지 | ✅ | `reports/page.tsx:190-191` | 선택 전 안내 |
| Edge Case: `markup_usd = 0` 표시 유지 | ✅ | `reports/page.tsx:150-152` | 0.0000으로 표시 |
| Edge Case: 빈 `by_model` → "모델 데이터 없음" | ✅ | `reports/page.tsx:239-243` | 조건부 메시지 |
| Edge Case: 팀 목록 로딩 실패 에러 표시 | ⚠️ | `reports/page.tsx:24-27` | 빈 목록으로 폴백되나 에러 메시지 없음 |

---

## Code Quality Findings

### Critical
없음

### Warning

**W1. CSV 내보내기 에러 미노출** (`reports/page.tsx:35-50`)
`handleCsvExport`에 `catch` 블록이 없어 내보내기 실패 시 사용자에게 아무 피드백이 없음.
`finally`에서 `setExporting(false)`는 처리되지만 실패 원인이 화면에 노출되지 않음.

**W2. 팀 목록 로딩 실패 미처리** (`reports/page.tsx:24-27`)
`teams.list()` 실패 시 `teamList`가 `[]`로 폴백되어 빈 select만 표시됨.
플랜 Edge Case 5에서 "에러 상태 표시"를 명시했으나 구현 없음.
운영자가 팀 목록 비어있음과 API 오류를 구분할 수 없음.

### Info

**I1. React key로 배열 index 사용** (`reports/page.tsx:225`)
`showback.by_model.map((m, i) => <tr key={i} ...)` — `m.Model`을 key로 사용하는 것이 더 안전.

**I2. DOM 미첨부 `<a>` 태그 클릭** (`reports/page.tsx:41-46`)
`a.click()` 패턴은 대부분의 현대 브라우저에서 동작하나 일부 구형 환경에서 DOM 첨부 후 클릭을 요구할 수 있음. 현재 타깃 환경에서는 문제없을 것으로 판단.

**I3. PascalCase 필드명** (`api.ts:752-760`)
`BillingModelBreakdown.Model`, `CostUSD`, `Tokens` — Go 백엔드의 json tag 부재로 인한 불가피한 선택.
코드 주석으로 이유가 명시되어 있어 문서화 양호.

---

## Positive Notes

- 플랜의 UI 레이아웃 명세(Header → Period → Tab → Table)를 정확히 구현
- 기존 `StatCard` 컴포넌트 재사용으로 일관된 디자인 패턴 유지
- `BillingModelBreakdown` 네이밍으로 기존 `ModelBreakdown` 충돌 회피 및 주석으로 이유 명시
- Showback 미선택 상태 등 UX 엣지 케이스 대부분 처리
- 테스트 파일(`ReportsPage.test.tsx`) 7개 케이스로 핵심 렌더링 시나리오 커버

---

*Reviewed by Claude Code*
